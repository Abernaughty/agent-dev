{
  "$schema": "MCP server version pinning - review monthly, never auto-update",
  "last_reviewed": "2026-03-24",
  "servers": {
    "filesystem": {
      "package": "@anthropic/mcp-filesystem",
      "version": "0.6.2",
      "integrity": "TODO: add sha256 hash after first install",
      "purpose": "Read/write access to project code",
      "notes": "Scoped to workspace directory only"
    },
    "github": {
      "package": "@anthropic/mcp-github",
      "version": "0.6.2",
      "integrity": "TODO: add sha256 hash after first install",
      "purpose": "Manage PRs, issues, diffs",
      "notes": "Requires GITHUB_TOKEN in environment"
    }
  }
}
