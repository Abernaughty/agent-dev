**Summary:** Applied a single-line change in `dashboard/src/lib/components/BottomPanel.svelte` inside the `handleMouseMove` function. Replaced the hard-coded `400` pixel cap in `Math.min(400, ...)` with `window.innerHeight * 0.8`, so the bottom panel can now resize up to 80% of the viewport height instead of being capped at 400px. The 60px minimum floor and all other code remain unchanged.